'use client'
import { useEffect, useState } from 'react'
import { useQuery } from '@tanstack/react-query'
import { BrainCircuit, Camera, CheckCircle2, Gauge, ShieldAlert, Sparkles, Video, AlertCircle, ArrowUpRight, BookOpenCheck, Target, TrendingUp } from 'lucide-react'
import { Bar, BarChart, CartesianGrid, Line, LineChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts'
import { aiApi } from '@/services/aiApi'
import { useAuthStore } from '@/store/authStore'
import { useNavigate, useParams } from 'react-router-dom'
import { Card, Header } from '@/components/shared'
import { useMonitorStore } from '@/store/monitorStore'
import { useExamMonitor } from '@/hooks/useExamMonitor'
import type { AnalyticsOverview } from '@/types/ai'
const overview: AnalyticsOverview = { completionRate: 78, averageScore: 82, flaggedAttempts: 7, activeSessions: 24, scoreTrend: [{label:'Mon',value:74},{label:'Tue',value:79},{label:'Wed',value:77},{label:'Thu',value:84},{label:'Fri',value:82}], difficultyTrend: [{label:'Easy',value:31},{label:'Medium',value:49},{label:'Hard',value:20}], riskDistribution: [{label:'Low',value:68},{label:'Medium',value:24},{label:'High',value:8}] }
const fallbackStudentAnalytics = { readiness: 82, strongestArea: 'Concept application', nextFocus: 'Timed recall', recommendations: ['Review error patterns from the last two exams.', 'Practice 10-minute recall drills before each session.', 'Attempt medium-difficulty questions with a time limit.'], strengths: ['Concept application', 'Core terminology', 'Pattern recognition'], weakAreas: ['Timed recall', 'Multi-step reasoning', 'Edge cases'], performanceTrend: [{ label: 'Mon', value: 74 }, { label: 'Tue', value: 79 }, { label: 'Wed', value: 77 }, { label: 'Thu', value: 84 }, { label: 'Fri', value: 82 }], topicPerformance: [{ topic: 'Foundations', score: 91, questions: 18 }, { topic: 'HTTP & APIs', score: 84, questions: 14 }, { topic: 'Architecture', score: 71, questions: 12 }, { topic: 'Testing', score: 64, questions: 9 }], difficultyAnalysis: [{ level: 'Easy', score: 94, attempts: 20 }, { level: 'Medium', score: 82, attempts: 28 }, { level: 'Hard', score: 68, attempts: 14 }], recentExams: [{ exam: 'Foundations assessment', score: 86, date: 'Today' }, { exam: 'Web architecture quiz', score: 79, date: 'May 18' }, { exam: 'API design checkpoint', score: 88, date: 'May 11' }] }
export function StudentAIAnalysis() { const user = useAuthStore.getState().user; const query = useQuery({ queryKey: ['student-ai-analysis', user?.id ?? 'demo-student'], queryFn: async () => (await aiApi.studentAnalytics(user?.id ?? 'demo-student')).data, retry: 1 }); const data = query.data ?? fallbackStudentAnalytics; if (query.isPending) return <><Header title="AI Performance Analysis" description="Personalized insights from your latest examination activity."/>{query.isError && <div className="mb-5 flex items-center gap-3 rounded-2xl border border-destructive/30 bg-destructive/5 p-4 text-destructive"><AlertCircle size={20}/><div><p className="font-medium">Live analysis is temporarily unavailable</p><p className="mt-1 text-sm text-muted-foreground">Showing the latest local preview until the Spring Boot analytics endpoint responds.</p></div></div>}<div className="grid gap-4 md:grid-cols-3">{[1, 2, 3].map(item => <Card key={item}><div className="h-20 animate-pulse rounded-xl bg-muted"/></Card>)}</div></>;  return <><Header title="AI Performance Analysis" description="Personalized insights from your latest examination activity."/>{query.isError && <div className="mb-5 flex items-center gap-3 rounded-2xl border border-destructive/30 bg-destructive/5 p-4 text-destructive"><AlertCircle size={20}/><div><p className="font-medium">Live analysis is temporarily unavailable</p><p className="mt-1 text-sm text-muted-foreground">Showing the latest local preview until the Spring Boot analytics endpoint responds.</p></div></div>}<div className="grid gap-4 md:grid-cols-3"><Card><div className="flex items-center justify-between"><div><p className="text-sm text-muted-foreground">Overall readiness</p><p className="mt-2 text-3xl font-semibold">{data.readiness}%</p></div><Sparkles className="text-primary"/></div><div className="mt-4 h-2 rounded-full bg-muted"><div className="h-2 rounded-full bg-primary" style={{ width: `${data.readiness}%` }}/></div></Card><Card><div className="flex items-center justify-between"><div><p className="text-sm text-muted-foreground">Strongest area</p><p className="mt-2 font-semibold">{data.strongestArea}</p></div><Gauge className="text-primary"/></div><p className="mt-4 text-xs text-muted-foreground">Consistently above your average</p></Card><Card><div className="flex items-center justify-between"><div><p className="text-sm text-muted-foreground">Next focus</p><p className="mt-2 font-semibold">{data.nextFocus}</p></div><Target className="text-primary"/></div><p className="mt-4 text-xs text-muted-foreground">Highest-impact improvement area</p></Card></div><Card className="mt-5"><div className="flex items-center justify-between gap-4"><div><h2 className="font-semibold">Personalized recommendations</h2><p className="mt-1 text-sm text-muted-foreground">Small, focused actions generated from your recent results.</p></div><BookOpenCheck className="text-primary"/></div><div className="mt-5 grid gap-3 md:grid-cols-3">{data.recommendations.map((item, index) => <div key={item} className="rounded-xl bg-muted p-4 text-sm leading-6"><div className="mb-3 flex size-8 items-center justify-center rounded-lg bg-primary/10 text-primary">{index + 1}</div>{item}<ArrowUpRight className="mt-4 text-primary" size={17}/></div>)}</div></Card><div className="mt-5 grid gap-5 xl:grid-cols-[1.25fr_.75fr]"><Card><div className="flex items-center justify-between"><div><h2 className="font-semibold">Performance trend</h2><p className="mt-1 text-sm text-muted-foreground">Your five most recent activity points</p></div><TrendingUp className="text-primary"/></div><div className="mt-5 h-64 min-w-0"><ResponsiveContainer width="100%" height="100%"><LineChart data={data.performanceTrend}><CartesianGrid strokeDasharray="3 3" vertical={false} stroke="hsl(var(--border))"/><XAxis dataKey="label" tickLine={false} axisLine={false}/><YAxis domain={[50, 100]} tickLine={false} axisLine={false}/><Tooltip/><Line type="monotone" dataKey="value" stroke="hsl(var(--primary))" strokeWidth={3} dot={{ r: 4 }}/></LineChart></ResponsiveContainer></div></Card><Card><h2 className="font-semibold">Strengths</h2><div className="mt-5 space-y-3">{data.strengths.map(item => <div key={item} className="flex items-center gap-3 rounded-xl bg-muted p-3 text-sm"><CheckCircle2 className="shrink-0 text-emerald-600" size={18}/>{item}</div>)}</div><h2 className="mt-7 font-semibold">Weak areas</h2><div className="mt-4 space-y-3">{data.weakAreas.map(item => <div key={item} className="flex items-center gap-3 rounded-xl bg-muted p-3 text-sm"><span className="size-2 shrink-0 rounded-full bg-amber-500"/>{item}</div>)}</div></Card></div><div className="mt-5 grid gap-5 lg:grid-cols-2"><Card><h2 className="font-semibold">Topic-wise performance</h2><div className="mt-5 h-64 min-w-0"><ResponsiveContainer width="100%" height="100%"><BarChart data={data.topicPerformance} layout="vertical" margin={{ left: 12, right: 12 }}><CartesianGrid strokeDasharray="3 3" horizontal={false} stroke="hsl(var(--border))"/><XAxis type="number" domain={[0, 100]} hide/><YAxis type="category" dataKey="topic" width={86} tickLine={false} axisLine={false}/><Tooltip/><Bar dataKey="score" fill="hsl(var(--primary))" radius={[0, 6, 6, 0]}/></BarChart></ResponsiveContainer></div></Card><Card><h2 className="font-semibold">Difficulty analysis</h2><div className="mt-5 space-y-5">{data.difficultyAnalysis.map(item => <div key={item.level}><div className="mb-2 flex justify-between text-sm"><span>{item.level}</span><span className="font-medium">{item.score}% <span className="text-muted-foreground">· {item.attempts} attempts</span></span></div><div className="h-2 rounded-full bg-muted"><div className="h-2 rounded-full bg-primary" style={{ width: `${item.score}%` }}/></div></div>)}</div></Card></div><Card className="mt-5"><div className="flex items-center justify-between"><h2 className="font-semibold">Recent exam performance</h2><span className="text-xs text-muted-foreground">Latest results</span></div><div className="mt-4 overflow-x-auto"><div className="min-w-[520px] divide-y divide-border">{data.recentExams.map(item => <div key={`${item.exam}-${item.date}`} className="flex items-center justify-between gap-4 py-4"><div><p className="text-sm font-medium">{item.exam}</p><p className="mt-1 text-xs text-muted-foreground">{item.date}</p></div><span className="font-semibold text-primary">{item.score}%</span></div>)}</div></div></Card></> }
export function AdaptiveExam() { const { id = '1' } = useParams(); const navigate = useNavigate(); const [index, setIndex] = useState(0); const [difficulty, setDifficulty] = useState(2); const [selected, setSelected] = useState(''); const items = [{ text: 'Which protocol secures HTTP traffic?', options: ['FTP','TLS','SMTP','DNS'] }, { text: 'Which layer manages reliable delivery?', options: ['Transport','Presentation','Physical','Session'] }, { text: 'What does a cache primarily improve?', options: ['Latency','Typography','Authorization','Storage cost'] }]; const item = items[index]; return <><Header title="Adaptive practice" description={`Difficulty adjusts as you answer. Session ${id}.`}/><Card className="mx-auto max-w-3xl"><div className="flex items-center justify-between text-sm text-muted-foreground"><span>Question {index + 1} of {items.length}</span><span className="rounded-full bg-primary/10 px-3 py-1 text-primary">Level {difficulty}</span></div><div className="mt-3 h-2 rounded-full bg-muted"><div className="h-2 rounded-full bg-primary" style={{ width: `${((index + 1) / items.length) * 100}%` }}/></div><h2 className="mt-10 text-xl font-semibold">{item.text}</h2><div className="mt-6 grid gap-3">{item.options.map((option) => <button type="button" key={option} aria-pressed={selected === option} onClick={() => setSelected(option)} className={`rounded-xl border p-4 text-left text-sm transition-[background-color,border-color,box-shadow,transform] duration-150 active:scale-[0.99] focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring ${selected === option ? 'border-primary bg-primary/10 shadow-sm' : 'border-border'}`}>{option}</button>)}</div><div className="mt-8 flex justify-between"><button className="rounded-xl border px-4 py-2 text-sm" disabled={!index} onClick={() => setIndex(index - 1)}>Previous</button><button className="rounded-xl bg-primary px-4 py-2 text-sm text-primary-foreground" onClick={() => { setDifficulty(selected === item.options[1] ? Math.min(5, difficulty + 1) : Math.max(1, difficulty - 1)); setSelected(''); if (index === items.length - 1) navigate('/student/history'); else setIndex(index + 1) }}>{index === items.length - 1 ? 'Finish' : 'Next question'}</button></div></Card></> }
export function ProctorMonitor() { const { id = 'attempt-1' } = useParams(); useExamMonitor(id); const { webcam, events, setWebcam } = useMonitorStore(); useEffect(() => { setWebcam({ supported: !!navigator.mediaDevices?.getUserMedia }); }, [setWebcam]); return <><Header title="Live proctoring monitor" description="Review session health and risk signals from the active exam."/><div className="grid gap-5 lg:grid-cols-[1.2fr_.8fr]"><Card><div className="flex items-center justify-between"><div><p className="text-sm text-muted-foreground">Session status</p><p className="mt-1 flex items-center gap-2 font-semibold text-emerald-600"><span className="size-2 rounded-full bg-emerald-500"/> Monitoring active</p></div><ShieldAlert className="text-amber-600"/></div><div className="mt-6 grid gap-3 sm:grid-cols-3"><div className="rounded-xl bg-muted p-4"><p className="text-xs text-muted-foreground">Risk score</p><p className="mt-1 text-2xl font-semibold">Low</p></div><div className="rounded-xl bg-muted p-4"><p className="text-xs text-muted-foreground">Events</p><p className="mt-1 text-2xl font-semibold">{events.length}</p></div><div className="rounded-xl bg-muted p-4"><p className="text-xs text-muted-foreground">Connection</p><p className="mt-1 text-2xl font-semibold">Good</p></div></div><div className="mt-6 space-y-3">{events.length ? events.map((event, index) => <div key={`${event.type}-${index}`} className="flex justify-between rounded-xl border border-border p-3 text-sm"><span>{event.type.replaceAll('_', ' ')}</span><span className="text-muted-foreground">{new Date(event.timestamp).toLocaleTimeString()}</span></div>) : <p className="rounded-xl bg-muted p-4 text-sm text-muted-foreground">No proctoring events detected.</p>}</div></Card><Card><div className="flex items-center gap-2"><Video className="text-primary"/><h2 className="font-semibold">Webcam status</h2></div><div className="mt-5 grid place-items-center rounded-2xl bg-slate-950 py-16 text-slate-300"><Camera size={32}/><p className="mt-3 text-sm">Camera preview ready</p></div><p className="mt-4 text-sm text-muted-foreground">{webcam.supported ? 'Browser camera support detected. Connect the backend stream to enable live frames.' : 'Camera support is unavailable in this browser.'}</p></Card></div></> }
export function AdminAnalytics() { return <><Header title="AI analytics" description="Understand performance, adaptive difficulty, and examination risk at a glance."/><div className="grid gap-4 md:grid-cols-4">{[['Completion rate', `${overview.completionRate}%`], ['Average score', `${overview.averageScore}%`], ['Flagged attempts', overview.flaggedAttempts], ['Active sessions', overview.activeSessions]].map(([label, value]) => <Card key={label}><p className="text-sm text-muted-foreground">{label}</p><p className="mt-2 text-3xl font-semibold">{value}</p></Card>)}</div><div className="mt-5 grid gap-5 lg:grid-cols-2"><Card><h2 className="font-semibold">Score trend</h2><div className="mt-6 flex h-40 items-end gap-3">{overview.scoreTrend.map((point) => <div key={point.label} className="flex flex-1 flex-col items-center gap-2"><div className="w-full rounded-t-lg bg-primary" style={{ height: `${point.value}%` }}/><span className="text-xs text-muted-foreground">{point.label}</span></div>)}</div></Card><Card><h2 className="font-semibold">Risk distribution</h2><div className="mt-6 space-y-4">{overview.riskDistribution.map((point) => <div key={point.label}><div className="mb-1 flex justify-between text-sm"><span>{point.label}</span><span>{point.value}%</span></div><div className="h-2 rounded-full bg-muted"><div className="h-2 rounded-full bg-primary" style={{ width: `${point.value}%` }}/></div></div>)}</div></Card></div></> }
