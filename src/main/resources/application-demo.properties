# ------------------------------------------------------------------
# Demo profile: seeds development-only convenience accounts.
# Activate with SPRING_PROFILES_ACTIVE=demo.
# Do NOT enable this profile in production.
# ------------------------------------------------------------------
spring.sql.init.data-locations=classpath:data-demo.sql